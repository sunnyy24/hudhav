import Link from "next/link";
import { ArrowLeft, ExternalLink, ShieldCheck } from "lucide-react";
import { SiteHeader } from "@/components/site-header";

export default function AboutPage() {
  return <main className="aimd-shell min-h-screen"><SiteHeader /><div className="aimd-container pb-24 pt-14 sm:pt-20"><Link href="/" className="inline-flex items-center gap-2 text-xs font-semibold text-slate-500 hover:text-white"><ArrowLeft size={14} /> Dashboard</Link><div className="mt-12 max-w-3xl"><div className="eyebrow">About AIMD</div><h1 className="mt-4 text-5xl font-semibold tracking-[-0.045em] text-white sm:text-6xl">Digital forensics for a synthetic media world.</h1><p className="mt-6 text-lg leading-8 text-slate-400">AIMD helps investigators understand what an image can and cannot tell them. It connects machine learning with artifact-level evidence and makes uncertainty part of the product.</p></div><div className="mt-14 grid gap-4 md:grid-cols-3"><div className="panel p-6"><ShieldCheck className="text-signal" size={22} /><h2 className="mt-8 font-semibold text-white">Evidence first</h2><p className="mt-3 text-sm leading-6 text-slate-500">The interface keeps raw signals and explanations close to the verdict.</p></div><div className="panel p-6"><ShieldCheck className="text-signal" size={22} /><h2 className="mt-8 font-semibold text-white">No false certainty</h2><p className="mt-3 text-sm leading-6 text-slate-500">Unavailable or conflicting evidence produces an honest inconclusive assessment.</p></div><div className="panel p-6"><ShieldCheck className="text-signal" size={22} /><h2 className="mt-8 font-semibold text-white">Built to extend</h2><p className="mt-3 text-sm leading-6 text-slate-500">The pipeline leaves room for calibrated validation, provenance, video, and audio analysis.</p></div></div><div className="mt-14 border-t border-line pt-8"><div className="eyebrow">Project references</div><div className="mt-5 flex flex-wrap gap-3"><Link href="/methodology" className="inline-flex items-center gap-2 border border-line px-4 py-3 text-sm text-slate-300 hover:border-signal hover:text-signal">Read methodology <ExternalLink size={14} /></Link><Link href="/investigate" className="inline-flex items-center gap-2 bg-signal px-4 py-3 text-sm font-bold text-ink">Start investigation <ExternalLink size={14} /></Link></div></div></div></main>;
}
